import styled from 'styled-components'
import {maxWidth, f_blanc} from '../../components/variables'
export const AboutContainer = styled.div`
    max-width: ${maxWidth};
    margin: 0 auto;
    width:100%;
    background-color: ${f_blanc};
    padding: 4rem 0;
 
`